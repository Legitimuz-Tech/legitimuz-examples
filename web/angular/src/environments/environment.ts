// Angular não lê .env. O caminho da própria ferramenta é o arquivo de environment,
// trocado no build por fileReplacements.
export const environment = {
  production: false,
  sdkUrl: "<SUA_SDK_URL>",
};
